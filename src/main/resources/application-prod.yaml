spring:
  datasource:
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/newspaper
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect