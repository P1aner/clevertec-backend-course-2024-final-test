spring:
  profiles:
    active:
      - dev
  jpa:
    hibernate:
      ddl-auto: validate
    generate-ddl: true
    show-sql: true
    database-platform: database-platform
server:
  port: 2000
app:
  cache:
    maxSize: 3
    algorithm: LRU